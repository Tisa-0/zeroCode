# DataEase 二次开发预研清单

## 功能模块一：缺失值填充

### 1. 功能概述

在数据集字段批量管理中新增"缺失值填充"功能，支持对某列的值进行缺失值填充处理。

### 2. 涉及文件清单

#### 2.1 新增文件

| 序号 | 文件路径 | 说明 |
|------|----------|------|
| 1 | `core/core-frontend/src/views/visualized/data/dataset/form/FillNullFieldEdit.vue` | 缺失值填充对话框组件 |

#### 2.2 修改文件

| 序号 | 文件路径 | 修改内容 |
|------|----------|----------|
| 1 | `core/core-frontend/src/views/visualized/data/dataset/form/index.vue` | 添加导入、状态变量、方法、对话框、操作按钮 |
| 2 | `core/core-frontend/src/locales/zh-CN.ts` | 添加国际化文本 |

### 3. 功能入口点

| 位置 | 操作方式 | 说明 |
|------|----------|------|
| 批量管理 - 维度表格 | 点击操作列"缺失值填充"按钮 | 对维度字段进行填充 |
| 批量管理 - 指标表格 | 点击操作列"缺失值填充"按钮 | 对指标字段进行填充 |
| 数据预览 - 表头 | 点击表头列，选择"缺失值填充" | 通过下拉菜单打开 |

### 4. 核心实现细节

#### 4.1 新增组件 FillNullFieldEdit.vue

```
功能：缺失值填充对话框
├── 原始字段信息展示（只读）
├── 字段名称输入（必填）
├── 填充策略下拉框
│   ├── 固定值（需输入自定义值）
│   ├── 空字符串
│   ├── 0
│   ├── 平均值（仅数值字段）
│   ├── 前一个值
│   ├── 后一个值
│   └── 众数（最频繁值）
├── 自定义填充值输入（策略为"固定值"时显示）
└── 原值下拉框（可选）
    ├── 空（NULL）
    └── 空字符串
```

**关键方法：**
- `initEdit(obj, availableFields, nullValues)` - 初始化弹窗数据
- `getResult()` - 获取填充配置结果
- `handleStrategyChange()` - 处理策略切换

#### 4.2 index.vue 新增内容

**状态变量：**
```typescript
const fillNullTitle = ref('')
const fillNullEdit = ref()
const editFillNullField = ref(false)
```

**新增方法：**
```typescript
- getNullValuesForField(field) - 获取字段的缺失值列表
- addFillNullFieldWithField(field) - 打开缺失值填充弹窗
- closeFillNullField() - 关闭弹窗
- confirmFillNullField() - 确认填充，创建新字段
- onPreviewColumnCommand(column, command) - 处理预览表头下拉菜单命令
```

**新增对话框：**
```vue
<el-dialog custom-class="fill-null-field-dialog">
  <fill-null-field-edit ref="fillNullEdit" />
</el-dialog>
```

#### 4.3 填充配置数据结构

```typescript
{
  id: string,           // 字段ID
  name: string,         // 新字段名称
  originName: string,   // 原始名称
  groupType: 'd' | 'q', // 维度或指标
  deType: number,       // 字段类型
  extField: 2,          // 计算字段标记
  fillConfig: {
    sourceFieldId: string,      // 源字段ID
    sourceFieldName: string,    // 源字段名称
    fillStrategy: string,       // 填充策略
    customValue: string,        // 自定义填充值
    selectedNullType: string    // 选中的原值类型
  }
}
```

### 5. 填充策略说明

| 策略 | 值 | 说明 | 适用字段 |
|------|-----|------|----------|
| 固定值 | `fixed` | 使用用户输入的自定义值填充 | 所有类型 |
| 空字符串 | `empty_string` | 使用空字符串填充 | 文本类 |
| 0 | `zero` | 使用0填充 | 所有类型 |
| 平均值 | `avg` | 使用平均值填充 | 数值类型 |
| 前一个值 | `forward` | 使用前一个非空值填充 | 所有类型 |
| 后一个值 | `backward` | 使用后一个非空值填充 | 所有类型 |
| 众数 | `mode` | 使用最频繁出现的值填充 | 所有类型 |

### 6. 原值类型说明

| 类型 | 值 | 说明 |
|------|-----|------|
| 空（NULL） | `null` | 数据库中的NULL值 |
| 空字符串 | `empty_string` | 空字符串 "" |

### 7. 国际化文本

```typescript
{
  fill_null_field: '缺失值填充',
  fill_strategy: '填充策略',
  custom_fill_value: '自定义填充值',
  input_fill_value: '请输入填充值',
  null_value_selection: '原值（可选）',
  null_value_placeholder: '不选则替换所有空值',
  origin_field: '原始字段',
  please_select_field: '请选择一个字段'
}
```

### 8. 后续工作建议

| 序号 | 工作项 | 优先级 | 说明 |
|------|--------|--------|------|
| 1 | 后端 SQL/计算逻辑实现 | 高 | 实现实际的填充 SQL 生成逻辑 |
| 2 | 前端预览数据填充展示 | 中 | 在数据预览中显示填充后的效果 |
| 3 | 填充字段编辑功能 | 低 | 支持编辑已创建的填充字段配置 |
| 4 | 英文/繁体国际化 | 中 | 完善多语言支持 |
| 5 | 单元测试 | 中 | 添加组件和方法的测试用例 |

### 9. 技术参考

**类似功能参考：**
- `GroupFieldEdit.vue` - 值分组功能（类似的弹窗编辑模式）
- `CalcFieldEdit.vue` - 计算字段编辑功能

**相关API：**
- `fetchFieldValuesFromApi(field)` - 获取字段值列表（已有）
- `multFieldValuesForPermissions` - 批量获取字段枚举值（已有）

---

## 附录：代码修改汇总

### A. FillNullFieldEdit.vue 完整代码结构

```
├── <script>
│   ├── 导入依赖
│   ├── 填充策略配置 (fillStrategies)
│   ├── 原值类型选项 (nullTypeOptions)
│   ├── 默认表单 (defaultForm)
│   ├── 响应式状态 (state)
│   ├── 计算属性 (availableStrategies, needCustomValue)
│   ├── 方法 (initEdit, selectField, handleStrategyChange, getResult)
│   └── 暴露方法 (defineExpose)
├── <template>
│   └── el-form
│       ├── 原始字段信息
│       ├── 字段名称输入
│       ├── 填充策略下拉框
│       ├── 自定义填充值输入
│       └── 原值下拉框
└── <style>
    └── 组件样式
```

### B. index.vue 修改位置

| 行数范围 | 修改内容 |
|----------|----------|
| ~25 | 导入 FillNullFieldEdit 组件 |
| ~690 | 添加状态变量 (fillNullTitle, fillNullEdit, editFillNullField) |
| ~673 | 添加 onPreviewColumnCommand 方法 |
| ~756-840 | 添加 getNullValuesForField, addFillNullFieldWithField, closeFillNullField, confirmFillNullField 方法 |
| ~2686 | 预览表头添加 el-dropdown 菜单 |
| ~2861 | 维度表格操作列添加"缺失值填充"按钮 |
| ~3050 | 指标表格操作列添加"缺失值填充"按钮 |
| ~3234 | 添加 fill-null-field-dialog 对话框 |

---

*文档生成时间：2026-03-20*
